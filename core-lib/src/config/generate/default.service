[Unit]
Description=Starts hyprshell daemon
After=graphical-session.target
Wants=graphical-session.target
StartLimitIntervalSec=600
StartLimitBurst=5

[Service]
Type=simple
ExecStart={path} run {params}
Restart=always
{extra}

[Install]
WantedBy=graphical-session.target
